pub strut Varilla{
    id: usize,
    nombre: String,
    marca: String,
    costo: f32,
}

impl Varialla{
    pub fn varilla_grid(ancho: f32, largo:f32, separacion: f32){
        let cnt_ancho = ancho / separacion;
        let cnt_largo = largo / separacion;
    }
}